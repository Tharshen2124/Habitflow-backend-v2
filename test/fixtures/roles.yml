# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  user: one
  role_name: Professional
  icon_id: briefcase

two:
  user: two
  role_name: Parent
  icon_id: users

# users(:three)'s pair: one standing, one archived. The archived one still holds a goal in the past
# week -- the case /history exists to keep visible and every planning read deliberately hides.
standing:
  user: three
  role_name: Programmer
  icon_id: code
  color_id: primary

retired:
  user: three
  role_name: Volunteer
  icon_id: heart
  color_id: teal
  deleted_at: <%= Time.current.to_fs(:db) %>

# users(:four) holds the whole carry-forward chain under one role, so the lineage is the only thing
# the tests around it vary.
persistent:
  user: four
  role_name: Researcher
  icon_id: code
  color_id: primary
